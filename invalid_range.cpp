#include <iostream>

int main() {

    // problem with char
    for (char a = 0; a <= 255; a++) {
        //std::cout << a;
    }

    // problem with bool
    for (bool a = 1; a <= 5; a++) {
        std::cout << a;
    }

  

    return 0;
}